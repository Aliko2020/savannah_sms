# Database configuration for local Docker
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=5432
DB_HOST=localhost

DATABASE_URL
# Application Security
NODE_ENV=production
JWT_SECRET=
PORT=

# Super Admin seed data
SEED_ADMIN_USERNAME=
SEED_ADMIN_PASSWORD=

# mNotify SMS
MNOTIFY_API_KEY=
MNOTIFY_SENDER_ID=

# Admin/Super Admin login 2FA (SMS code). Set to "false" to temporarily
# disable during testing — set back to "true" (or remove) to re-enable.
LOGIN_OTP_ENABLED=false